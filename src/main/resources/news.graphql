schema {
    query: Query
}

type Query {
    allNewsItems: [NewsItem]
    newsItem(link: String): NewsItem
}

type NewsItem {
    guid: String
    title: String
    description: String
    publishedDate: String
    imageUrl: String
}